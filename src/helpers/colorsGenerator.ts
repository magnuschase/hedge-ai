import ColorHash from 'color-hash'

export const COLOR_GEN_DARK = new ColorHash({
	lightness: 0.25,
	saturation: 0.3
})

export const COLOR_GEN_LIGHT = new ColorHash({
	lightness: 0.8,
	saturation: 0.3
})
